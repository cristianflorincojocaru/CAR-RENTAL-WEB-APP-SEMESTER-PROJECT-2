// src/environments/environment.ts
export const environment = {
  production: false,
  // URL-ul backend-ului ASP.NET Core în development
  // Schimbă portul dacă este necesar (launchSettings.json din backend)
  apiUrl: 'https://localhost:7273/api'
};
